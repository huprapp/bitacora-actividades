
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: redirige cualquier ruta a index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

